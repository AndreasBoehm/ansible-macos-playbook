---

- name: Install gnu-sed.
  homebrew:
    name: gnu-sed
    state: latest

- name: Install vim.
  homebrew:
    name: vim
    state: latest

- name: Install htop.
  homebrew:
    name: htop
    state: latest

- name: Install java 8 JDK via adoptopenjdk cask.
  homebrew_cask:
    name: adoptopenjdk8
    state: installed

